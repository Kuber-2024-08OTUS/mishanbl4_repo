apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homework-pvc
  namespace: homework
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: homework-storage-class
  resources:
    requests:
      storage: 1Gi